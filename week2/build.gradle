
dependencies {
    compile project(":commons")
}

task minSkewJar(type: Jar, dependsOn: build) {
    doFirst {
        from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
    }
    exclude 'META-INF/*.RSA', 'META-INF/*.SF', 'META-INF/*.DSA'
    manifest {
        attributes "Main-Class": "com.zymosi3.bioinf.week2.MinimumSkew"
    }
    archiveName = "minskew.jar"
    destinationDir = file("${buildDir}/libs")
    from(sourceSets.main.output) {}
    // creating fat jar
    from {
        configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
    }
}

task salmonellaEntericaJar(type: Jar, dependsOn: build) {
    doFirst {
        from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
    }
    exclude 'META-INF/*.RSA', 'META-INF/*.SF', 'META-INF/*.DSA'
    manifest {
        attributes "Main-Class": "com.zymosi3.bioinf.week2.SalmonellaEnterica"
    }
    archiveName = "salmonellaEnterica.jar"
    destinationDir = file("${buildDir}/libs")
    from(sourceSets.main.output) {}
    // creating fat jar
    from {
        configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
    }
}